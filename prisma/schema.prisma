generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model chopinLiszt {
  id_number       Int     @id
  checklist_item  String  @unique
  checked         Boolean
}
