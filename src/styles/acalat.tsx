import { createTheme } from "@mantine/core";

export const acalat = createTheme({
  primaryColor: "orange",

  // Set rounding for buttons and inputs
  defaultRadius: "lg",

  // Set cursor, mainly for the checkbox list
  cursorType: "pointer",
});
